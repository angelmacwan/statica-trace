/**
 * App.tsx — placeholder shell for the Statica Trace frontend.
 * Full implementation is in Module 4.
 */
export default function App() {
  return (
    <main>
      <h1>Statica Trace</h1>
      <p>Agent replay debugger — loading…</p>
    </main>
  );
}
